# Calcit 0.13.29 upgrade

- Resolved the upgrade branch against current `main` and updated the dependency snapshot to Calcit 0.13.29.
- Kept the canonical `calcit.cirru` snapshot, removed legacy `compact.cirru`, and migrated snapshot guidance to current Calcit commands.
- Updated CI to `setup-calcit` and added format, default/test-entry check-only, and JavaScript codegen gates before the Rust build.
- Verified the snapshot formatting and local Calcit checks with the latest installed tool.
