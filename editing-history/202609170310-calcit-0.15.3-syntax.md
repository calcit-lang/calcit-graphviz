# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将 Calcit 与 `@calcit/procs` 固定到 0.15.3，并升级已发布的 `calcit.std` 0.2.32。
- 使用 `surface-latest-v2` 检查完整 Snapshot；当前没有需要应用的语法改写。
- 保持纯 Calcit DOT 生成 API 与输出不变。

## English

- Pin Calcit and `@calcit/procs` to 0.15.3 and upgrade to the published `calcit.std` 0.2.32.
- Check the full Snapshot with `surface-latest-v2`; no syntax rewrite is currently required.
- Preserve the pure-Calcit DOT generation API and output.

## 验证 / Verification

- strict dependency and toolchain verification
- default and `run-tests` entry checks, behavior tests, type/quality and dynamic-method gates
- native and JavaScript code generation
- deterministic DOT rendering and Vite production build
