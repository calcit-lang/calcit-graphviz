# Release 0.0.7 / 发布 0.0.7

Tracks calcit-lang/calcit#835.

- Publish the strict-validated Graphviz module with the immutable `calcit.std` 0.2.31 dependency.
- 发布经过严格门禁验证、并固定不可变 `calcit.std` 0.2.31 依赖的 Graphviz 模块。
- Includes the canonical guide and native demo boundary fixes already merged through their focused pull requests.
- 包含此前通过独立 PR 合并的 canonical guide 与 native demo 边界修复。
- Release metadata only; no source, API, or workflow changes are introduced by this commit.
- 本提交仅包含发布 metadata，不引入源码、API 或 workflow 变更。
